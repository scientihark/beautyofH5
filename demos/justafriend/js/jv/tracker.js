define([ 'jv/data/config'], function (google_analytics, config) {

    

    return {

        trackView:function (pageName, isPassive) {

           
        },

        // Parameters:
        // el - the element that triggered this action
        // type - 'o' for onsite/other, 'e' for exit link & 'd' for download link
        // name - name of the event
        // eventID - the eventID of the event being used
        // eVar - The name of the eVar variable to send
        // prop - The name of the prop variable to send
        trackEvent: function(el, type, name, eventID, eVar, prop) {

           
        }

    }

});